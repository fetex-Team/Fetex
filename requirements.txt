# Python 3.12에서 검증한 핵심 실행 의존성
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
h3>=4.0
pygeohash>=2.1
matplotlib>=3.8
networkx>=3.2
pyproj>=3.6
eclipse-sumo>=1.27.1
sumolib>=1.27.1
traci>=1.27.1
# rtree: sumolib의 net.getNeighboringEdges()가 쓰는 공간 인덱스.
#   없으면 경고를 내고 전수 탐색으로 대체된다. 결과는 같고 속도만 느려진다.
rtree>=1.0

# Module 2 (전처리/EDA)
holidays
statsmodels

# 노트북 (notebooks/ 실행용)
jupyter
nbconvert
ipykernel

# CNN-LSTM 학습은 requirements-cnn.txt 추가 설치 (torch)
