# 통합 분석 보고서 (작성 중)

## 2. 시공간 데이터 특성 분석 (EDA) — Module 2

> 상세 그림·표는 `notebooks/01_EDA_and_Spatial.ipynb` (실행 결과 포함). 아래는 요약.

### 2.1 데이터
- 강남역 SUMO 시뮬레이션 호출 로그 1일(2026-09-07 월) 09:00~11:00, 2,870건. 한계: 1일치 → 요일·날씨 효과 분석은 다일 데이터 확보 후(매뉴얼 T7).

### 2.2 시간 패턴
- 5분 단위 총 호출: 09:00~09:55 상승(출근 규칙), 10:00 이후 급락. 시뮬 규칙이 데이터에 재현됨을 확인.

### 2.3 공간 격자 — H3 resolution 9 채택
| res | 셀 수 | 변 길이 | 0인 칸 | 최다 셀 점유 | lag-1 r |
|---|---|---|---|---|---|
| 8 | 7 | 531m | 11.9% | 35.1% | 0.60 |
| **9** | **22** | **201m** | **20.5%** | **16.7%** | **0.30** |
| 10 | 70 | 76m | 37.0% | 8.0% | 0.17 |
- 8은 공간 정보 부족(too coarse), 10은 희소·노이즈(too fine). 근거: `data/eda/h3_resolution_compare.png`

### 2.4 POI zone 검증
- 매핑 반경은 결과에 영향 없음(40~200m 동일). 겹침은 큰길에 POI 집중 + zone 내 균등 랜덤 선택 때문. 주택 POI가 큰길로 매핑되는 문제 → Module 1 공유.

### 2.5 정상성·자기상관·피처 유효성
- ADF 검정, ACF, 피처 그룹 ablation 결과: 노트북 §5~§7 (다일 데이터로 재실행 후 확정 수치 기입)

### 2.6 외부 데이터 병합
- 1시간 날씨 관측 → 5분 칸: `merge_asof(backward, tolerance 3h)`, 짧은 공백 보간(≤3h) + `weather_interpolated`, 긴 공백 `weather_missing` 플래그, 파일 없으면 상수 fallback. 검증: `tests/test_module2.py` T4.
