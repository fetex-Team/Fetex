# 통합 분석 보고서 (작성 중)

## 2. 시공간 데이터 특성 분석 (EDA) — Module 2

> 상세 그림·표는 `notebooks/01_EDA_and_Spatial.ipynb` (실행 결과 포함). 아래는 요약.

### 2.1 데이터
- 강남역 SUMO 시뮬레이션 호출 로그 1일(2026-09-07 월) 09:00~11:00, 2,870건. 한계: 1일치 → 요일·날씨 효과 분석은 다일 데이터 확보 후(매뉴얼 T7).

### 2.2 시간 패턴
- 5분 단위 총 호출: 09:00~09:55 상승(출근 규칙), 10:00 이후 급락. 시뮬 규칙이 데이터에 재현됨을 확인.

### 2.3 공간 격자 — H3 resolution 9 채택
| res | 셀 수 | 변 길이 | 0인 칸 | 최다 셀 점유 | lag-1 r |
|---|---|---|---|---|---|
| 8 | 7 | 531m | 11.9% | 35.1% | 0.60 |
| **9** | **22** | **201m** | **20.5%** | **16.7%** | **0.30** |
| 10 | 70 | 76m | 37.0% | 8.0% | 0.17 |
- 8은 공간 정보 부족(too coarse), 10은 희소·노이즈(too fine). 근거: `data/eda/h3_resolution_compare.png`

### 2.4 POI zone 검증
- 매핑 반경은 결과에 영향 없음(40~200m 동일). 겹침은 큰길에 POI 집중 + zone 내 균등 랜덤 선택 때문. 주택 POI가 큰길로 매핑되는 문제 → Module 1 공유.

### 2.5 정상성·자기상관·피처 유효성 (1일치 로그 기준 — 예비)
**정상성(ADF)**: 원계열 통계량 -1.073, p=0.726 → 비정상 / 1차 차분 -5.116, p<0.0001 → 정상.
수준이 시간에 따라 변하는(출근 창 열림·닫힘) 시계열이므로 차분 계열 피처(`diff_1`)와 이동평균으로 수준 변화를 흡수하는 설계가 타당. ARIMA 계열과 비교할 때 차분 차수 d=1의 근거.

**자기상관(ACF)**: 셀 평균 ACF에서 유의(±1.96/√24=±0.40) lag 0개. 시간칸 24개·셀당 수요 0~3건으로 기준선이 너무 높아 판정 불가 — 데이터 부족의 증거이지 lag 피처 무용의 증거가 아님.

**피처 유효성(ablation, XGBoost, 타겟 y_h1, test 22행=시간칸 1개)**
| 피처 그룹 | 피처 수 | RMSE | MAE |
|---|---|---|---|
| 나이브(다음 칸 = 직전 칸) | 1 | 1.758 | 1.273 |
| lag만 | 12 | 1.966 | 1.459 |
| +rolling/diff | 19 | 1.826 | 1.275 |
| +시간(calendar) | 29 | 1.759 | 1.241 |
| +날씨 | 40 | 1.759 | 1.241 |

학습 110행에 lag 12개는 과적합(나이브보다 나쁨)이고, 피처를 더할수록 나이브로 수렴. **이 데이터 크기에서는 피처 설계의 우열을 판정할 수 없다** → 다일 데이터 확보(매뉴얼 T7)가 선행 과제. 재실행은 `jupyter nbconvert --to notebook --execute --inplace notebooks/01_EDA_and_Spatial.ipynb`.

**대용량 처리**: 10만 행(중복 없는 최악 조건) H3+Geohash 변환 0.42초, 행 단위 apply 대비 2.3배. 실제 로그는 동일 좌표 반복이 많아 캐시 효과가 더 큼.

### 2.6 외부 데이터 병합
- 날씨: Open-Meteo Archive 시간별 관측(강남역 중심, 2026-08-31~09-10, 264시간) → `data/external/weather_강남역_*.csv`
- 1시간 관측 → 5분 칸: `merge_asof(backward, tolerance 3h)`, 짧은 공백 보간(≤3h) + `weather_interpolated`, 긴 공백 `weather_missing` 플래그, 파일 없으면 상수 fallback. 검증: `tests/test_module2.py` T4 (10개).
- 공휴일: `holidays` KR 달력 → `is_holiday`.
- 상관: 1일치 로그에서는 요일·공휴일이 단일값, 기온은 2시간 동안 22→25℃로 단조 증가라 시간대 효과와 분리 불가 → 다일 데이터 후 재분석.
